with(player)
{
	randomize();
}

/*if(sprite_index == spr_big_chaos)
{
	player.deck[player.deckCount++] = 3;
	scr_shuffle_deck();
	
	with(player)
	{
		alarm[6] = 1;
		handCount = 0;
		draw_phase = true;
		alarm[0] = 1;
	}
}
*/
